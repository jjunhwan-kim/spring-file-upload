spring:
  config:
    activate:
      on-profile: windows

  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB
      location: C:\\project\\temp

path:
  upload: C:\\project\\upload